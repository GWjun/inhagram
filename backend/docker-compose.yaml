services:
  postgres:
    image: postgres:16
    restart: always
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: your_name
      POSTGRES_PASSWORD: your_password
      POSTGRES_DB: your_db